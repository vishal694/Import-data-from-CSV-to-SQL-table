


CREATE TABLE PERSONS (
    PERSONID INT  NOT NULL,
    LASTNAME VARCHAR(255) NULL ,
    FIRSTNAME VARCHAR(255) NULL,
    ADDRESS VARCHAR(255) NOT NULL,
    CITY VARCHAR(255) NOT NULL,
	MOBILE_NO VARCHAR(255) NOT NULL,
	EMAIL VARCHAR(255) NOT NULL,
	TIME_STAMP DATE DEFAULT GETDATE(),
	USERNAME VARCHAR(255) DEFAULT 'SQL_IMPORT' NOT NULL,
	LAST_ACTION VARCHAR(255) DEFAULT 'INSERTED'
);

//make primary_key Constraint
ALTER TABLE PERSONS
ADD CONSTRAINT PERSONS_PK PRIMARY KEY (PERSONID, MOBILE_NO);